<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — İzin Bloğu
    Xiaomi Redmi Note 10 Pro (Android 13 / API 33) için gerekli izinler

    ANDROID 13 KIRICI DEĞİŞİKLİKLER (Desktop'ta bunlar yoktu):
    1. POST_NOTIFICATIONS — Bildirim göndermek için runtime permission gerekiyor
    2. READ_MEDIA_IMAGES   — Scoped Storage (READ_EXTERNAL_STORAGE artık çalışmıyor)
    3. SCHEDULE_EXACT_ALARM — Kesin zamanlayıcılar için yeni izin
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 13+ bildirim izni (runtime'da istenmeli) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Scoped Storage — üye fotoğrafları için (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- WorkManager kesin alarm için (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Ağ (ileride Firebase sync için) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".GymApplication"
        android:label="@string/app_name"
        android:theme="@style/Theme.GymApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WorkManager Hilt entegrasyonu -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

    </application>
</manifest>
